{
  "compilerOptions": {
    // --- Key changes to relax rules ---
    "noImplicitAny": false,     // Allows function parameters to have an implicit 'any' type. Fixes TS7031 & TS7006.
    "noUnusedLocals": false,    // Allows declared variables that aren't used. Fixes TS6133.
    "strict": false,            // Setting strict to false also relaxes many rules, including noImplicitAny.

    // --- Standard Vite + React settings ---
    "composite": true,
    "target": "ES2020",
    "useDefineForClassFields": true,
    "lib": ["ES2020", "DOM", "DOM.Iterable"],
    "module": "ESNext",
    "skipLibCheck": true,

    /* Bundler mode */
    "moduleResolution": "bundler",
    "allowImportingTsExtensions": true,
    "resolveJsonModule": true,
    "isolatedModules": true,
    "noEmit": true,
    "jsx": "react-jsx"
  },
  "include": ["src"]
}